import * as moment from 'moment';

export class Hours {
    bar: string;
    weekday: string;
    opening: moment.Moment; // make sure this works
    closing: moment.Moment; // make sure this works
}
